<h1>All Events:</h1>

<% @events.each do |e| %>
    <p><%= e.location %></p>
    <p><%= e.date %></p>
<% end %>

<% if @signed_in %>
    <%= button_to(
        "Log Out",
        destroy_user_session_path,
        method: :delete
    ) %>
    <h5> welcome <%= @user.email %> </h5>
<% end %>